# Query Patterns

- Architecture overview: use `get_architecture`.
- Module search: use `search_graph`.
- Text or symbol search: use `search_code`.
- Function flow: use `trace_path`.
- Diff impact: use `detect_changes` when available.

